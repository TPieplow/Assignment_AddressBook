<UserControl x:Class="Assignment_AdressBook.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assignment_AdressBook.Views"
             xmlns:viewmodels="clr-namespace:Assignment_AdressBook.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <StackPanel Margin="20">
            <Button Content="Add Contact" Width="100" Margin="5" Command="{Binding NavigateToAddContactCommand}"></Button>
            <Button Content="Get Contacts" Width="100" Margin="5" Command="{Binding NavigateToGetContactsCommand}"></Button>

            <Button Content="Get Contact" Width="100" Margin="5" Command="{Binding NavigateToCreateFileCommand}"></Button>
            <Button Content="Load File" Width="100" Margin="5" Command="{Binding NavigateToLoadFileCommand}"></Button>
            <Button Content="Create A File" Width="100" Margin="5" Command="{Binding NavigateToCreateFile}"></Button>
        </StackPanel>
        
    </Grid>
</UserControl>
